# SECURITY-TODO — إصلاحات مؤجّلة تحتاج موافقة المالك

هذه بنود **لم تُطبّق** في جولة التنظيف الآمن (2026-09) لأنها قد تكسر التشغيل أو
تحتاج قراراً من المالك. لا تُطبَّق تلقائياً. لم يُلمس `server.ts` ولا طبقة Gemini
ولا المصادقة ولا `firestore.rules` (deny-all سليمة).

## 1) ترقية xlsx (ثغرات معروفة) — مؤجّلة
- `xlsx@0.18.5` في `package.json` به ثغرات معروفة (Prototype Pollution / ReDoS، GHSA-4r6h-8v6p-xvw6, GHSA-5pgg-2g8v-p4x9).
- **لم تُغيَّر النسخة** الآن لأن أحدث إصدارات xlsx تُوزَّع عبر CDN الخاص بالمشرّع وقد يتغيّر سطح الاستيراد ويكسر استيراد/تصدير الجداول.
- الإجراء المقترح (بموافقة): الترقية إلى إصدار مثبّت من مصدر SheetJS الرسمي ثم تشغيل اختبارات الاستيراد/التصدير قبل الدمج.

## 2) مفاتيح Firebase العامة في المستودع — مراجعة
- `firebase-applet-config.json` يحتوي `apiKey` و`oAuthClientId` و`recaptchaSiteKey` و`appId` وغيرها.
- هذه مفاتيح Firebase **من النوع العام (client-side)** ويُقصد كشفها في الواجهة، لكن يُفضّل:
  - التأكد أن حصر الوصول يتم عبر `firestore.rules` (deny-all) وقيود App Check/reCAPTCHA وليس عبر سرية المفتاح.
  - مراجعة `oAuthClientId` والتأكد من ضبط Authorized origins/redirect URIs بدقة.
  - النظر في نقل هذه القيم لمتغيّرات بيئة عند البناء بدل تثبيتها في المستودع (تحسين لا إصلاح عاجل).

## 3) تحديد المعدّل (rate limit) في الذاكرة لا يعمل عبر منطقتين
- `rateLimitLogin` في `server.ts` (حوالي السطر 319) تخزين **في الذاكرة** لكل نسخة (instance).
- عند التشغيل على أكثر من نسخة/منطقة (Cloud Run multi-instance/region) لا يُشارَك العدّاد، فيصبح الحدّ الفعلي = الحدّ × عدد النسخ، ما يضعف الحماية من التخمين/إساءة الاستخدام.
- الإجراء المقترح (بموافقة): نقل العدّاد لمخزن مشترك (Firestore/Redis/Memorystore) أو تفعيل حصر على مستوى الحافة (Cloud Armor).

## 4) لا حدّ على `/api/public/*`
- توجد نقاط نهاية عامة متعددة تحت `/api/public/*` بلا حدّ معدّل واضح (rateLimitLogin مطبّق على مسارات المصادقة فقط).
- الإجراء المقترح (بموافقة): إضافة تحديد معدّل/حماية على المسارات العامة، مع الانتباه لعدم كسر الاستخدام المشروع.

## 5) الاختبارات تعتمد على `database/db.json.gz` غير الموجود في CI
- سكربتات مثل `test:parity` / `migrate:firestore` تشير إلى `database/db.json.gz`.
- الملف **مستبعَد عمداً** من المستودع عبر `.gitignore` (يحتوي بيانات Legacy خاصة) وبالتالي **غير موجود في CI**، ما قد يجعل اختبارات المطابقة تُتخطّى أو تفشل في بيئة CI.
- الإجراء المقترح (بموافقة): توفير عيّنة مُصغّرة/مُعمّاة (fixture) للـCI، أو تخطّي واضح ومقصود لهذه الاختبارات عند غياب الملف.

## 6) ملفات OCR (`ara.traineddata` / `eng.traineddata`) — لم تُحذف عمداً
- `ara.traineddata` (~2.4MB) و`eng.traineddata` (~5.0MB) **لم تُحذف** رغم ضخامتها.
- السبب: `src/utils/documentOcr.ts` يستدعي `tesseract.js` عبر `createWorker(...)` بدون ضبط `langPath`/`cachePath`. الافتراضي في tesseract.js أن `cachePath = cwd`، و`Dockerfile` ينسخ الجذر إلى `/app` ويشغّل من `/app`، فيقرأ tesseract هذين الملفين من القرص كذاكرة تخزين مؤقتة **بدون تنزيل من CDN وقت التشغيل**.
- حذفهما يغيّر السلوك (تنزيل من الشبكة عند كل بدء بارد، وقد يفشل OCR إن كان الخروج للإنترنت محجوباً). لذلك أُبقيا.
- الإجراء المقترح (بموافقة، ليس إصلاحاً عاجلاً): نقلهما إلى مجلد بيانات مخصّص وضبط `langPath` صراحةً عليه، بدل تركهما في جذر المستودع.

## 7) تنظيف تاريخ git للملفات المحذوفة — يحتاج موافقة منفصلة
حذف الملفات من الشجرة يكفي لهذه الجولة، لكن **التاريخ ما زال يحتوي نسخها**. لم تُعَد
كتابة التاريخ (لا rebase/force-push) حسب قواعد التنظيف. الملفات التالية أُزيلت من الشجرة
وقد تستحق تنظيف تاريخ بموافقة المالك:
- `cookies.txt` — أثر جلسة (رأس ملف فقط بلا كوكيز فعلية، لكن يُفضّل إزالته من التاريخ).
- `room-registry-review.xlsx` — جدول مراجعة غرف قد يحوي بيانات؛ يُراجَع قبل تنظيف التاريخ.
- سكربتات فحص/ترقيع تجريبية (`test-*`, `.probe-*`, `.crop.ts`, `fix-server.command`, `metadata.json`, `bun.lock`).
- ملاحظة: `fix-server.command` كان يُنزّل `server.ts` من GitHub ويرقّعه محلياً — أُزيل من الشجرة.
