# الجدول — لم أمسّه بأمرك ✅

**المستودع:** `ProfAlfailakawi/schedule`

> قلتَ: «الجدول أموره ممتازة» — فتركتُه كما هو تماماً.

## ما رصدتُه — للعلم فقط، لا للتنفيذ الآن

`src/db/repository.ts:2978` — `getSchedules()` يقرأ ثلاث مجموعات كاملة
(`schedules` · `courses` · `sections`) بلا حدّ وبلا كاش، ويُستدعى من
**9 مسارات** في `server.ts`.

**لماذا لم ألمسه:** تمريره عبر الكاش يتطلب إضافة مفتاحه إلى قائمة
الإبطال عند **كل** كتابة جدول. لو فات مفتاح واحد، ظهرت بيانات قديمة
بعد التعديل — وهذا كسر صامت لا يُكتشف إلا متأخراً.

## ما هو ممتاز فعلاً في مستودعك

- ✅ المسار الساخن للمقررات **مُحسَّن مسبقاً** بفهرس مركّب وتقسيم دفعات
- ✅ كاش داخلي بمهلة 90 ثانية موجود ويعمل مع إبطال عند الكتابة
- ✅ **لا مستمعين حيّين إطلاقاً**
- ✅ النشر بلا نسخ دائمة — يتقلّص لصفر
- ✅ صرفه الفعلي: **3.5 دولار** فقط

## ما نُفِّذ
✅ حُذفت النسخة المهجورة `schedule-web` (أمريكا، بلا نطاق، متوقفة منذ 19 أغسطس)
✅ النسختان الحيّتان تعملان: `schedule-2.ai.studio` — HTTP 200
✅ تنبيه ميزانية مفعّل
